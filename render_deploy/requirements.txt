fastapi>=0.110
uvicorn[standard]>=0.27

# Pin Python to 3.12 on Render; this NumPy build is compatible there.
numpy==1.26.4
pillow>=10.4
pandas>=2.2
matplotlib>=3.8

# OpenCV: garder "contrib" < 4.12 (inclut opencv-core)
opencv-contrib-python==4.11.0.86
# NE PAS installer "opencv-python" en plus (doublon et conflits)
# opencv-python==4.11.0.86

mediapipe==0.10.35

# Vision/ML
torch
torchvision
timm>=0.9.0  # For Vision Transformer models
face_recognition>=1.3     # nécessite dlib (peut demander build tools sous Windows)

# Training dependencies
tqdm>=4.64
scikit-learn>=1.3
matplotlib>=3.8
pandas>=2.2
av>=10.0.0  # PyAV for video processing

# FastAPI utils
python-multipart>=0.0.9
pydantic>=2.6

# Crypto: n'ajoute PAS "Crypto" (module fourni par pycryptodome)
pycryptodome>=3.20

requests>=2.32
